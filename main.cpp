#include<iostream>
#include<random>
using namespace std;

int main() {
	// initializing control variables
  unsigned maxLen=10, maxHeight=10, noOfMines=20;

	// defining boards
	char gameBoard[maxLen][maxHeight];
	char displayBoard[maxLen][maxHeight];

	// initializing coords of 
	// initializing boards
	for(unsigned i{0};i<maxLen;i++)
		for(unsigned j{0};j<maxHeight;j++)
			

	return 0;
}
